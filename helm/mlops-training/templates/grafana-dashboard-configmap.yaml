{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: mlops-platform-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  mlops-platform-dashboard.json: |
{{ .Files.Get "dashboards/mlops-platform-dashboard.json" | indent 4 }}
{{- end }}